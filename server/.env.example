# Server removed — this project is now frontend-only.
# The server-based .env example has been removed. For the frontend-only flow,
# set VITE_SPOTIFY_CLIENT_ID in your deployment (for example, Vercel env vars)
# and add your deployed site root as the Redirect URI in the Spotify Dashboard.
